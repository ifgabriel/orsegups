import { StyleSheet } from 'react-native'


const styles = StyleSheet.create({
    title: {
        fontSize: 22,
        fontWeight: 'bold',
        color: '#000',
        marginBottom: 24,
    },
    container: {
        gap: 16,
        paddingVertical: 24,
        paddingHorizontal: 8,
    },
})

export { styles }

